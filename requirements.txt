# Sokuji-Bridge Requirements
# Core dependencies for running the translation system

# Core dependencies
numpy>=1.24.0
pydantic>=2.0.0
pyyaml>=6.0
python-dotenv>=1.0.0

# Async and API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0

# Audio processing
sounddevice>=0.4.6
soundfile>=0.12.1

# Logging
loguru>=0.7.2

# STT Provider: faster-whisper
faster-whisper>=0.10.0
# Alternative: openai-whisper>=20231117

# VAD (Voice Activity Detection) - Required for SimulStreaming with VAD enabled
# Note: Silero VAD is loaded automatically from torch.hub when vad_enabled=true
# torch>=2.5.0 is already listed below for NLLB translation

# Translation Provider: NLLB
transformers>=4.35.0
torch>=2.5.0,<2.6.0  # Version constraint for torchaudio compatibility
torchaudio>=2.5.0,<2.6.0  # Required for Silero VAD (automatic sentence segmentation)
sentencepiece>=0.1.99

# TTS Provider: Piper
piper-tts>=1.2.0

# Optional: GPU acceleration (CUDA 12.1+ / 13.0 compatible)
# If you have NVIDIA GPU, install PyTorch with CUDA support:
# For CUDA 12.1 / 13.0:
# pip install torch==2.5.1 torchvision==0.20.1 torchaudio==2.5.1 --index-url https://download.pytorch.org/whl/cu121
# For CPU only:
# pip install torch==2.5.1 torchvision==0.20.1 torchaudio==2.5.1 --index-url https://download.pytorch.org/whl/cpu

# Development dependencies (optional)
# pytest>=7.4.0
# pytest-asyncio>=0.21.0
# black>=23.0.0
# ruff>=0.1.0
# mypy>=1.7.0
