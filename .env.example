# Sokuji-Bridge Environment Configuration
# Copy this file to .env and customize for your deployment

# ============================================================================
# Provider Selection (build-time)
# Change these to switch providers, then rebuild the service
# ============================================================================

# STT Provider Options: faster_whisper (default), whisper_api, azure_stt
STT_PROVIDER=faster_whisper

# Translation Provider Options: nllb_local (default), deepl_api, openai_gpt4, google_translate
TRANSLATION_PROVIDER=nllb_local

# TTS Provider Options: piper (default), xtts, elevenlabs, kokoro
TTS_PROVIDER=piper

# ============================================================================
# Model Cache Directory
# ============================================================================
MODEL_CACHE_DIR=~/.cache/sokuji-bridge/models

# ============================================================================
# API Keys (only needed for API-based providers)
# ============================================================================

# OpenAI API (for whisper_api or openai_gpt4)
# OPENAI_API_KEY=your_openai_api_key_here

# DeepL API (for deepl_api)
# DEEPL_API_KEY=your_deepl_api_key_here

# ElevenLabs API (for elevenlabs)
# ELEVENLABS_API_KEY=your_elevenlabs_api_key_here

# Azure Speech Services (for azure_stt)
# AZURE_SPEECH_KEY=your_azure_key_here
# AZURE_SPEECH_REGION=your_azure_region_here

# ============================================================================
# Provider-Specific Configuration
# ============================================================================

# STT Model Configuration (for faster_whisper)
STT_MODEL_SIZE=medium
STT_DEVICE=cuda
STT_COMPUTE_TYPE=float16
STT_VAD_FILTER=true

# Translation Model Configuration (for nllb_local)
TRANSLATION_MODEL=facebook/nllb-200-distilled-1.3B
TRANSLATION_DEVICE=cuda
TRANSLATION_PRECISION=float16

# TTS Model Configuration (for piper)
TTS_MODEL=en_US-lessac-medium
TTS_DEVICE=cpu

# ============================================================================
# Logging
# ============================================================================
LOG_LEVEL=INFO

# ============================================================================
# Example Configurations
# ============================================================================

# Configuration 1: All Local (High Performance, GPU Required)
# STT_PROVIDER=faster_whisper
# TRANSLATION_PROVIDER=nllb_local
# TTS_PROVIDER=xtts

# Configuration 2: All Cloud (No GPU Needed, API Costs)
# STT_PROVIDER=whisper_api
# TRANSLATION_PROVIDER=deepl_api
# TTS_PROVIDER=elevenlabs
# OPENAI_API_KEY=your_key
# DEEPL_API_KEY=your_key
# ELEVENLABS_API_KEY=your_key

# Configuration 3: Hybrid (Balanced)
# STT_PROVIDER=faster_whisper   # Local STT (slowest step)
# TRANSLATION_PROVIDER=deepl_api # Cloud translation (fastest)
# TTS_PROVIDER=piper              # Local CPU TTS (fast enough)
# DEEPL_API_KEY=your_key
